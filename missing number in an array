#include<stdio.h>
int main()
{
    int n,arr[10];
    scanf("%d",&n);
    int i,sum =0,arrtot , total;
    for(int i=0;i<n-1;i++)
    {
        scanf("%d",&arr[i]);
        sum += arr[i];
    }
    arrtot=n*(n+1)/2;
    total = arrtot - sum ;
    printf("%d",total);
}

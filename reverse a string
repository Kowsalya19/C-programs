#include<stdio.h>
#include<string.h>
int main()
{
    char a[100];
    int i=0,j;
    fgets(a,sizeof(a),stdin);
    j= strlen(a)-1;
    while(i<j)
    {
        char temp = a[i];
        a[i]=a[j];
        a[j]= temp;
        i++;
        j--;
    }
    printf("%s",a);
}
